'use strict';

var fs = require('fs');
var appPath = process.cwd();
var contentPath = appPath+'/panel/system/server/content';
var fs = require('fs');
var path = require('path');

module.exports = function(app, io, db){


	io.on('connection', function(socket){

	
	});

};
